module.exports = class Interactions{
    constructor(date, userWhoInteracted, userShown, liked) {
        this.date = date
        this.userWhoInteracted = userWhoInteracted
        this.userShown = userShown
        this.liked = liked
    }
}